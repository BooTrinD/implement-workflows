name: build
on: [push, pull_request]


jobs:
    build: 
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4
            - run: cmake -S . -B build
            - run: cmake --build build
            - uses: actions/upload-artifact@v4 
              with: 
                name: binaries 
                path: build/cpu_monitor_app
    test:
        needs: build
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4
            - uses: actions/download-artifact@v4
            - run: cmake -S . -B build
            - run: cmake --build build
            - run: ctest --test-dir build --output-on-failure